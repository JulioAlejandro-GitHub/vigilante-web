VITE_API_BASE_URL=http://127.0.0.1:8000

# Mock session defaults. Identity, role and org/site context can be changed in the UI and persist in localStorage.
VITE_DEFAULT_USER=julio
VITE_DEFAULT_ORGANIZATION_ID=
VITE_DEFAULT_SITE_ID=
